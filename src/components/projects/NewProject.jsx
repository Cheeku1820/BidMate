/* Placeholder — Task 7 builds the real create-project form (spec §5,
   screen B). This exists only so routes.jsx has something to route to. */
export default function NewProject() {
  return <h1>New project</h1>;
}
