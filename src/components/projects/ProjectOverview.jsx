/* Placeholder — Task 8 builds the real project overview (spec §6). This
   exists only so routes.jsx has something to route to. */
export default function ProjectOverview() {
  return <h1>Project overview</h1>;
}
