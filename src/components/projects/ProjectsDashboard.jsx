/* Placeholder — Task 7 builds the real projects dashboard (spec §5,
   screen A). This exists only so routes.jsx has something to route to
   and routes.test.jsx can assert the shell renders around it; the
   heading is the one thing the route test depends on. */
export default function ProjectsDashboard() {
  return <h1>Projects</h1>;
}
